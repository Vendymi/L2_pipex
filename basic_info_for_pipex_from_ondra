1. how to read infile + errors if not existing or permissions (outfile for pemissions too)
2. check if command is valid


open / read / write 


< infile grep r | wc -l > outfile 
cat outfile = 13

./pipex infile "grep r" "wc -l" outfile 

env | grep PATH=/nfs
PATH=/nfs/homes/vmichali/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin

grep cat echo ls wc chmod tail head


CodeVault - youtube 
